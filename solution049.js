/*Make a function that will return a greeting statement that uses an input; your program should return, "Hello, <name> how are you doing today?".*/

function greet(name){
    //P: use template literal to insert name argument as a variable in the string
    return `Hello, ${name} how are you doing today?`
  }

  //P: a string, never empty, no numbers
  //R: return "Hello, name how are you doing today?" with name as a variable
  //E: "Hello, MK how are you doing today?"