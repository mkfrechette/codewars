/*Input: Array of elements

["h","o","l","a"]

Output: String with comma delimited elements of the array in th same order.

"h,o,l,a"*/

//Join array into a string with each element separated by a comma
let printArray = array => array.join(',')